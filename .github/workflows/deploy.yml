name: Codex Apply Visible Build Update

on:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: codex-apply-visible-build
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Download Codex patch
        run: curl -fL "https://files.catbox.moe/8e6see.zip" -o codex-visible-build.zip

      - name: Apply scoped patch
        run: |
          python - <<'PY'
          import pathlib
          import zipfile

          allowed = {
            "index.html",
            "src/data/buildInfo.js",
            "src/components/StartScreen.jsx",
            "src/components/Hud.jsx",
            "src/styles/app.css",
          }
          found = set()
          root = pathlib.Path.cwd()

          with zipfile.ZipFile("codex-visible-build.zip") as archive:
            for info in archive.infolist():
              normalized = info.filename.replace("\\", "/").lstrip("/")
              if not normalized or normalized.endswith("/"):
                continue
              if normalized not in allowed:
                raise SystemExit(f"Unexpected file in patch: {normalized}")
              target = root / normalized
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_bytes(archive.read(info))
              found.add(normalized)

          missing = allowed - found
          if missing:
            raise SystemExit(f"Missing expected files: {sorted(missing)}")
          PY

      - name: Commit scoped patch
        run: |
          git config user.name "Codex"
          git config user.email "codex@openai.com"
          git rm -f .github/codex-apply-trigger.txt 2>/dev/null || rm -f .github/codex-apply-trigger.txt
          git add index.html src/data/buildInfo.js src/components/StartScreen.jsx src/components/Hud.jsx src/styles/app.css
          if git diff --cached --quiet; then
            echo "No scoped changes to commit."
            exit 0
          fi
          git commit -m "Add visible Pages build marker"
          git push origin HEAD:main
