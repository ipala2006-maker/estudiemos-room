name: Apply Estudiemos Room Patch

on:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: apply-estudiemos-room-patch
  cancel-in-progress: false

jobs:
  apply-patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download patch
        run: |
          curl -L "https://litter.catbox.moe/y3il3l.gz" -o /tmp/estudiemos-room-patch.tar.gz
          mkdir -p /tmp/estudiemos-room-patch
          tar -xzf /tmp/estudiemos-room-patch.tar.gz -C /tmp/estudiemos-room-patch

      - name: Apply files
        run: |
          cp -R /tmp/estudiemos-room-patch/. .
          git status --short

      - name: Commit patch and restore deploy workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/assets/dachshund-mascot-render.jpg src/components/DachshundMascot.jsx src/components/Hud.jsx src/styles/app.css src/styles/camera-controls.css src/data/buildInfo.js .github/workflows/deploy.yml
          if git diff --cached --quiet; then
            echo "No file changes to commit."
          else
            git commit -m "Compact guide and upgrade dachshund mascot"
            git push
          fi
