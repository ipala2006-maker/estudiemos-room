name: Apply Estudiemos Room Dark Computer Grass Patch

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply patch
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/estudiemos-patch"
          curl -L --fail --retry 3 -o "$RUNNER_TEMP/estudiemos-patch/patch.tar.gz" "https://litter.catbox.moe/83urz6.gz"
          tar -xzf "$RUNNER_TEMP/estudiemos-patch/patch.tar.gz" -C "$RUNNER_TEMP/estudiemos-patch"
          cp "$RUNNER_TEMP/estudiemos-patch/src/components/ComputerUI.jsx" "src/components/ComputerUI.jsx"
          cp "$RUNNER_TEMP/estudiemos-patch/src/components/FirstPersonWorld.jsx" "src/components/FirstPersonWorld.jsx"
          cp "$RUNNER_TEMP/estudiemos-patch/src/styles/computer-os-corrections.css" "src/styles/computer-os-corrections.css"
          cp "$RUNNER_TEMP/estudiemos-patch/src/data/buildInfo.js" "src/data/buildInfo.js"
          cp "$RUNNER_TEMP/estudiemos-patch/.github/workflows/deploy.yml" ".github/workflows/deploy.yml"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/ComputerUI.jsx src/components/FirstPersonWorld.jsx src/styles/computer-os-corrections.css src/data/buildInfo.js .github/workflows/deploy.yml
          git commit -m "fix: dark computer and simplify neighborhood" || exit 0
          git push
