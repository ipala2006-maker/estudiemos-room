# Estudiemos Room architecture library

These GLB files are original, project-owned modular assets generated with
Blender. They replace procedural architectural boxes while the game's
collision and interaction systems remain in JavaScript.

Source and rebuild instructions:

`tools/blender/`

Modules use meters, Y-up after glTF export, base-aligned pivots, named PBR
materials and compact generated textures.
